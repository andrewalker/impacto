    - create pages for
        - sale
        - purchase
        - consignation
        - return

    - subforms!
        - breadcrumbs
        - parent
            url
        - children
            item in the menu

    - product.return field 'confirmed'
        - two forms for return
            - one for employees, with confirmed set to true
            - one for representant, with confirmed set to false

    - field factory bytea to display image when editing
    - create a page to manage users
    - create a page for user's settings
