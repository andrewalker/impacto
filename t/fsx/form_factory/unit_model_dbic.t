#!/usr/bin/env perl
use warnings;
use strict;
use utf8;
use Test::More;
use Bread::Board;
#use Form::SensibleX::FormFactory::Model::DBIC;

# only to test *something*
use_ok( 'Form::SensibleX::FormFactory::Model::DBIC' );

done_testing();
