# backup files (if any)
*~

# hidden files
.*

# except this file, of course!
!.gitignore

# downloaded by script/impacto_get_dojo.sh
# plus stuff installed using cpm (dgrid, etc)
root/static/lib/

# ctags-exuberant tags file can be manually generated so it doesn't need to be
# in the vcs
tags

# Devel::Cover directory
cover_db/

# local config
impacto_local.*

# sqlite database for testing (generated by t/00create-db.t)
t/db/test.db

# tmp directory
tmp/
