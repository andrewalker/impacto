<:
cascade base::forms {
    form_name => 'product.product_category',
}

block form -> {
    combobox_factory_field('product', required(true))
    combobox_factory_field('category', required(true))
}
:>
