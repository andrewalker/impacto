    - Test *Base::CRUD
        - create a loop to request list / create / update in every CRUD controller
    - Documentation (and others!)
        - I::CB::Base
            - global_base
        - I::CB::CRUD
            - create
            - crud_base
            - crud_base_with_id
            - get_all_columns
            - list
            - list_json_data
            - make_form_action
            - update
        - I::CR::DataGrid
            - get_all_columns
            - get_browse_structure
            - get_elastic_search_insert_data
        - I::CR::Form
            - build_form_sensible_object
            - get_all_columns
            - render_form
            - submit_form
            - submit_form_create
            - submit_form_update
    - Implement search

    - Customize all child controllers
    - Put actual links in the menu
    - Install and use CatalystX::SimpleLogin

    - Create a page to manage users
    - Create a page for user's settings
    - Install on server

    - Deal with foreign keys and other extra params for forms
        # this is stuck, so I'll delay it a little
